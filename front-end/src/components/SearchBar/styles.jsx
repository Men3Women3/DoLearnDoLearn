import styled from "styled-components";

export const SLabel = styled.label`
  position: relative;

  .search-bar {
    border: none;
    /* padding: 1% 10%; // 안쪽 여백 */
    margin: 3% 15%; // 바깥쪽 여백
    height: 40px;
    border-color: black;
  }
  .glass-button {
    /* position: relative; */
    top: 0;
    right: 5px;
  }
`;
